apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: kate-auth-deployment
  labels:
    gitRev: "b5b13fa"
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      name: kate-auth
      labels:
        app: kate-auth
    spec:
      imagePullSecrets:
        - name: cpy-docker-test-registry
      containers:
      - name: kate-auth
        image: elmarweber/kamon-akka-http-tracing-kate-auth:1.0.0-b5efbaa
        imagePullPolicy: Always
        ports:
        - containerPort: 9090
        env:
        args: [
          "-Djava.security.egd=file:/dev/./urandom",
          "-Xmx1g",
          "-Dservice.interface.port=9090",
          "-Dkamon.jaeger.host=jaeger-agent.jaeger.svc.cluster.local",
        ]
